# V1.25.12 safe start - decomposition HTML

## Statut

Chantier demarre, non livrable stable.

## Base

- Branche source : `dev/v1.25.11-lan-stable-base`
- Base stable : `V1.25.11.1`
- Monolithe de rollback : `client/FC_LA_COUR_Manager.html`
- SHA-256 attendu : `5646baa6ab0d19c31172eeeb5270fcccc8bc719726cb2f9c06f4147677f1f45c`

## Changements initiaux

- Creation de la branche `refactor/v1.25.11.1-html-decomposition-safe`.
- Ajout de `docs/HTML_DECOMPOSITION_SAFE_START.md`.
- Ajout de `client/pages/README.md`.
- Ajout des premieres pages cibles en mode `legacy_bridge`.
- Ajout de `tests/test_safe_page_decomposition.py`.

## Extraction shadow ajoutee

`client/pages/admin/dashboard.html` est maintenant en `extracted_shadow`.

La page :

- conserve un lien visible vers `/gestion#dashboard` ;
- charge `client/shared/css/page-shell.css` ;
- charge `client/shared/js/dashboard-summary.js` ;
- lit `/api/session` et `/api/state/summary` ;
- reste en lecture seule ;
- ne devient pas encore le chemin principal.

## Pages encore en legacy bridge

- `client/pages/admin/members.html`
- `client/pages/admin/teams.html`
- `client/pages/public/home.html`
- `client/pages/coach/home.html`
- `client/pages/member/home.html`

## Garantie actuelle

Le monolithe n'est pas modifie. Les nouvelles pages ne remplacent pas encore `/gestion`. Le dashboard est maintenant testable physiquement comme page shadow, et le Manager stable reste le rollback fonctionnel.

## Tranche physique testable

Le serveur local expose maintenant :

- `/pages/admin/dashboard.html` apres connexion ;
- `/shared/css/page-shell.css` apres connexion ;
- `/shared/js/dashboard-summary.js` apres connexion ;
- `/gestion` sans changement comme rollback.

La page d'accueil connectee ajoute un bouton `Dashboard extrait` afin de tester la decomposition sans remplacer le Manager complet.

## Tests ajoutes

`tests/test_server.py` verifie : session requise, dashboard chargeable, CSS/JS servis, traversal refuse, CSP sans inline pour la page shadow, et monolithe toujours disponible via `/gestion`.

## Prochaine etape

Extraire une deuxieme page metier en shadow, probablement `client/pages/admin/members.html`, avec lecture API existante `/api/state/members` et lien de rollback vers `/gestion#members`.
