# V1.25.12 - test physique decomposition

## Statut

Version de chantier testable physiquement, non stable.

## Branche a tester

`refactor/v1.25.11.1-html-decomposition-safe`

Base de rollback conservee : `V1.25.11.1` via `client/FC_LA_COUR_Manager.html` et route `/gestion`.

## Pages shadow disponibles

| Page | URL locale | Donnees | Rollback |
| --- | --- | --- | --- |
| Dashboard admin | `/pages/admin/dashboard.html` | `/api/state/summary` | `/gestion#dashboard` |
| Licencies admin | `/pages/admin/members.html` | `/api/state/members?limit=500` | `/gestion#members` |

## Verification Windows locale

1. Mettre a jour le dossier `D:\FC_LA_COUR_GestionClub` depuis cette branche.
2. Lancer `LANCER_TESTS.cmd`.
3. Lancer `DEMARRER_SERVEUR.cmd`.
4. Ouvrir `http://127.0.0.1:8765/`.
5. Se connecter avec un compte serveur.
6. Cliquer `Dashboard extrait`.
7. Tester directement `http://127.0.0.1:8765/pages/admin/members.html`.
8. Verifier que les liens `Rollback Manager stable` ouvrent toujours le Manager complet.
9. Verifier que `http://127.0.0.1:8765/gestion` reste fonctionnel.

## Verification reseau local

Si le lanceur reseau est utilise, refaire les memes controles avec l'adresse LAN du poste serveur, par exemple :

- `http://192.168.1.3:8766/pages/admin/dashboard.html`
- `http://192.168.1.3:8766/pages/admin/members.html`
- `http://192.168.1.3:8766/gestion`

## Criteres de retour arriere

Revenir au Manager complet si :

- une page shadow affiche moins de donnees que `/gestion` ;
- une page shadow ne charge pas ses assets CSS/JS ;
- une session expiree donne un comportement ambigu ;
- les tests serveur echouent.

Dans tous les cas, `/gestion` reste le chemin metier principal jusqu'a validation physique explicite.
